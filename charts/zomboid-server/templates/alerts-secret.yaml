{{- if and .Values.alerts.enabled (not .Values.alerts.secretRef) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "zomboid-server.fullname" . }}-discord
  labels:
    {{- include "zomboid-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  DISCORD_WEBHOOK_URL: {{ .Values.alerts.discordWebhook | quote }}
{{- end }}

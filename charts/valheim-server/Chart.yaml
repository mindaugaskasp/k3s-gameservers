apiVersion: v2
name: valheim-server
description: Valheim dedicated server as a single-replica StatefulSet, backed by the community valheim-server-docker image.
type: application
version: 0.1.0
# Tracks the image tag in values.yaml, which is itself "latest" -- the
# image self-updates the game via its own UPDATE_CRON.
appVersion: "latest"
keywords:
  - valheim
  - game-server
  - k3s
home: https://github.com/community-valheim-tools/valheim-server-docker

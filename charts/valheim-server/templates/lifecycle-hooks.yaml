{{- if .Values.alerts.enabled }}
{{- if not .Values.alerts.secretRef }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "valheim-server.fullname" . }}-discord
  labels:
    {{- include "valheim-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  DISCORD_WEBHOOK_URL: {{ .Values.alerts.discordWebhook | quote }}
{{- end }}
---
{{- end }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "valheim-server.fullname" . }}-hooks
  labels:
    {{- include "valheim-server.labels" . | nindent 4 }}
data:
  # SOURCED by valheim-server's PRE_SERVER_RUN_HOOK, never executed: only a
  # same-shell run can clear DOORSTOP_ENABLED before the server launches.
  mod-guard.sh: |
    # No set -e and no exit in here -- both would take down the caller's shell.
    mod_guard() {
      [ "${BEPINEX:-false}" = true ] || return 0
      [ "${MOD_FAIL_SAFE:-false}" = true ] || return 0

      status_dir="/var/run/valheim-status"
      doorstop_lib="/opt/valheim/bepinex/doorstop_libs/libdoorstop_x64.so"
      reason=""

      # The loader breaking is the failure that crash-loops the server, e.g.
      # a BepInEx build needing a newer GLIBC than this image has.
      if [ ! -f "$doorstop_lib" ]; then
        reason="libdoorstop is missing"
      elif ldd "$doorstop_lib" 2>&1 | grep -q "not found"; then
        reason="libdoorstop has unresolved libraries: $(ldd "$doorstop_lib" 2>&1 | grep 'not found' | awk '{print $1}' | tr '\n' ' ')"
      fi

      if [ -z "$reason" ] && [ "${MOD_REQUIRE_BLESSED_BUILD:-false}" = true ]; then
        manifest=$(find /opt/valheim -maxdepth 4 -name 'appmanifest_896660.acf' 2>/dev/null | head -1)
        current=""
        [ -n "$manifest" ] && current=$(grep -m1 '"buildid"' "$manifest" | grep -oE '[0-9]+' | tail -1)
        blessed=$(cat /config/mods-blessed-build 2>/dev/null || true)
        if [ -z "$blessed" ]; then
          reason="no game build has been blessed for the installed mods"
        elif [ "$current" != "$blessed" ]; then
          reason="game build $current is not the blessed build $blessed"
        fi
      fi

      mkdir -p "$status_dir"
      if [ -n "$reason" ]; then
        # Clear both: with only DOORSTOP_ENABLED=0 an unloadable libdoorstop
        # would still be LD_PRELOADed and still kill the server.
        export DOORSTOP_ENABLED=0
        SERVER_LD_PRELOAD=""
        echo 0 > "$status_dir/mods-active"
        printf '%s' "$reason" > "$status_dir/mods-status"
        echo "mod-guard: starting WITHOUT mods -- $reason" >&2
        [ -x /etc/valheim-hooks/notify.sh ] && \
          /etc/valheim-hooks/notify.sh mods-disabled "$reason" || true
      else
        echo 1 > "$status_dir/mods-active"
        printf 'mods active' > "$status_dir/mods-status"
      fi
    }
    mod_guard
  bless-mods.sh: |
    #!/bin/sh
    # Records the running game build as validated, re-enabling mods on the
    # next start. Run via `make bless-mods` after testing the server modded.
    set -eu
    manifest=$(find /opt/valheim -maxdepth 4 -name 'appmanifest_896660.acf' 2>/dev/null | head -1)
    [ -n "$manifest" ] || { echo "no appmanifest found" >&2; exit 1; }
    build=$(grep -m1 '"buildid"' "$manifest" | grep -oE '[0-9]+' | tail -1)
    [ -n "$build" ] || { echo "could not read buildid" >&2; exit 1; }
    printf '%s' "$build" > /config/mods-blessed-build
    echo "blessed build $build; mods will load on next start"
  notify.sh: |
    #!/bin/sh
    # Records lifecycle timestamps for status-metrics and posts a Discord embed.
    # Not POST_START_HOOK: it fires on plain restarts, faking updates.
    set -eu
    status_dir="/var/run/valheim-status"
    persist_dir="/config"
    connect="${CONNECT_HOST:-}${CONNECT_HOST:+:${SERVER_PORT:-2456}}"

    json_escape() {
      printf '%s' "$1" | sed 's/\\/\\\\/g; s/"/\\"/g'
    }

    show_join=0
    case "$1" in
      started)
        date +%s > "$status_dir/last-started.timestamp"
        # Steam build ID, not the human "1.0.14" version -- only readable
        # from inside the container's install, gamedig cannot see it.
        manifest=$(find /opt/valheim -maxdepth 4 -name 'appmanifest_896660.acf' 2>/dev/null | head -1)
        if [ -n "$manifest" ]; then
          grep -m1 '"buildid"' "$manifest" | grep -oE '[0-9]+' | tail -1 > "$status_dir/build-id" || true
        fi
        title="Server online"; color=3066993; show_join=1
        desc="**${SERVER_NAME}** is up and accepting connections."
        ;;
      down)
        # Checkpoint this session into the PVC-backed running total;
        # Prometheus only retains 7d so it cannot answer lifetime uptime.
        started_at=$(cat "$status_dir/last-started.timestamp" 2>/dev/null || echo 0)
        case "$started_at" in ''|*[!0-9]*) started_at=0 ;; esac
        elapsed=0
        [ "$started_at" -gt 0 ] && elapsed=$(( $(date +%s) - started_at ))
        [ "$elapsed" -lt 0 ] && elapsed=0
        prior=$(cat "$persist_dir/uptime-accumulator.seconds" 2>/dev/null || echo 0)
        case "$prior" in ''|*[!0-9]*) prior=0 ;; esac
        echo $((prior + elapsed)) > "$persist_dir/uptime-accumulator.seconds"
        title="Server down"; color=15548997
        desc="**${SERVER_NAME}** is shutting down."
        ;;
      updated)
        date +%s > "$status_dir/last-updated.timestamp"
        title="Server updating"; color=5793266
        desc="**${SERVER_NAME}** was updated and is restarting."
        ;;
      mods-disabled)
        title="Mods disabled"; color=16705372
        desc="**${SERVER_NAME}** is starting without mods: ${2:-unknown reason}. The server is up; run \`make bless-mods\` once mods are verified."
        ;;
      *)
        title="Server event"; color=9807270
        desc="**${SERVER_NAME}**: $1"
        ;;
    esac

    if [ -n "${DISCORD_WEBHOOK_URL:-}" ]; then
      fields="[]"
      if [ -n "$connect" ]; then
        connect_field="{\"name\":\"Connect\",\"value\":\"$(json_escape "$connect")\",\"inline\":true},{\"name\":\"World\",\"value\":\"$(json_escape "${WORLD_NAME:-}")\",\"inline\":true}"
        join_field=""
        # Inline code, not a markdown link: Discord only auto-links
        # http(s), so [text](steam://...) renders as dead plain text.
        if [ "$show_join" = 1 ]; then
          join_field=",{\"name\":\"Join\",\"value\":\"\`steam://connect/$(json_escape "$connect")\`\",\"inline\":true}"
        fi
        fields="[${connect_field}${join_field}]"
      fi
      ts=$(date -u +%Y-%m-%dT%H:%M:%SZ)
      payload="{\"embeds\":[{\"title\":\"$(json_escape "$title")\",\"description\":\"$(json_escape "$desc")\",\"color\":$color,\"fields\":$fields,\"timestamp\":\"$ts\"}]}"
      curl -fsS -X POST -H "Content-Type: application/json" \
        -d "$payload" "${DISCORD_WEBHOOK_URL}" >/dev/null 2>&1 || true
    fi

apiVersion: 1
providers:
  # Files come from the dashboard sidecar; each subfolder becomes a Grafana folder.
  - name: sidecar
    type: file
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: true

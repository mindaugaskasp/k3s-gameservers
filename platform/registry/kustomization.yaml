apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: registry

labels:
  - pairs:
      app.kubernetes.io/name: registry
    includeSelectors: true

resources:
  - registry.yaml

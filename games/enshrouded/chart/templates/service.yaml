apiVersion: v1
kind: Service
metadata:
  name: {{ include "game-server.fullname" . }}
  labels:
    {{- include "game-server.labels" . | nindent 4 }}
spec:
  type: NodePort
  selector:
    {{- include "game-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: game
      port: {{ .Values.server.queryPort }}
      targetPort: {{ .Values.server.queryPort }}
      protocol: UDP
      nodePort: {{ .Values.service.nodePorts.game }}
